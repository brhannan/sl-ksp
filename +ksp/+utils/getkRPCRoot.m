function p = getkRPCRoot()
%GETKRPCROOT Return the path to the krpc folder.
%   P = KSP.UTILS.GETKRPCROOT() called with no input arguments returns the 
%   path to the krpc folder in character array P.
%
%   % EXAMPLE:
%       p = ksp.utils.getkRPCRoot()

% slkspRoot = ksp.utils.getPkgRoot();
% ... < path stuff goes here >

% hard code path (temporary ... fix this later)
p = '/Users/brianhannan/Documents/code/krpc';

end